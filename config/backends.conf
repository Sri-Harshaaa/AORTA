# One backend per line, as host:port.
# Lines starting with # are ignored.
#
# This is the local (non-Docker) list. Start backends with:
#   ./build/aorta --port 8081 --redis-host 127.0.0.1
127.0.0.1:8081
127.0.0.1:8082
127.0.0.1:8083
